/*
import (
	"encoding/json"
	"io/ioutil
)
bodyAsByteArray, err := ioutil.ReadAll(c.Request.Body)
	fmt.Println(bodyAsByteArray)
	if err != nil {
		fmt.Println(err);
	}
	jsonBody := string(bodyAsByteArray);
	fmt.Println(jsonBody);

	errr := json.NewDecoder(c.Request.Body);
	fmt.Println(errr)
*/